const { registerParticipant } = require('./lib/data/registrations.ts'); // Can't require TS directly like this in Node without transpilation.
// Let's just simulate the logic.
const events = require('./data/events.ts'); // Also TS.
